(() => {
  'use strict';

  const VERSION='20260901-searchindex1';
  const Contract=window.MatchCrossContract;
  if(!Contract){console.error('MatchCrossContract no está cargado.');return;}

  const state={excelFile:'',excelRecords:[],txtRecords:[],txtSources:new Map(),errors:[],discoveryIndex:Contract.buildDiscoveryIndex([],[])};
  const $=s=>document.querySelector(s);
  const esc=v=>String(v??'').replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;').replace(/'/g,'&#039;');
  const ext=name=>String(name||'').toLowerCase().split('.').pop();
  const uniqueFiles=files=>{const out=[],seen=new Set();for(const file of files||[]){if(!file||!/^txt$/i.test(ext(file.name)))continue;const key=`${file.webkitRelativePath||''}|${file.name}|${file.size}|${file.lastModified}`;if(seen.has(key))continue;seen.add(key);out.push(file);}return out;};
  const rebuildIndex=()=>{state.discoveryIndex=Contract.buildDiscoveryIndex(state.excelRecords,state.txtRecords);};

  function roleLabel(role){if(role==='CARCASA')return 'CARCASA';if(role==='EQUIPO')return 'EQUIPO';if(role==='ENCONTRADO')return 'ENCONTRADO PREVIO';return role||'REGISTRO';}
  function statusText(){
    const txtLines=[...state.txtSources.values()].reduce((n,x)=>n+x.records,0);
    const parts=[state.excelFile?`Base: ${state.excelFile} · ${state.excelRecords.length} filas indexadas`:'Base: no cargada',state.txtSources.size?`Comparación: ${state.txtSources.size} archivo${state.txtSources.size===1?'':'s'} · ${txtLines} líneas indexadas`:'Comparación: sin archivos cargados'];
    parts.push(`Índice: ${state.discoveryIndex.entries.length} valores`);
    if(state.errors.length)parts.push(`${state.errors.length} advertencia${state.errors.length===1?'':'s'}`);
    return parts.join(' · ');
  }
  function updateStatus(){const el=$('#crossMatchStatus');if(!el)return;const ready=Boolean(state.discoveryIndex.entries.length);el.className=`cross-match-status${ready?' ready':''}${state.errors.length?' warning':''}`;el.innerHTML=`<span class="cross-match-dot"></span><span>${esc(statusText())}</span>`;el.title=state.errors.slice(0,20).map(e=>`${e.file||''}${e.sheet?` · ${e.sheet}`:''}${e.lineNumber?` · línea ${e.lineNumber}`:''}: ${e.message}`).join('\n');}
  function refreshSearch(){if(window.MatchSearchModes&&typeof window.MatchSearchModes.executeSearch==='function')window.MatchSearchModes.executeSearch();}

  function ingestWorkbookModel(fileName,sheets){
    const parsed=Contract.parseWorkbookModel(fileName,sheets);state.excelFile=String(fileName||'Archivo de base');state.excelRecords=parsed.records;state.errors=state.errors.filter(e=>e.scope!=='EXCEL');state.errors.push(...parsed.errors.map(e=>({...e,scope:'EXCEL'})));rebuildIndex();updateStatus();refreshSearch();return parsed;
  }
  async function loadExcelFile(file){if(!file)return null;if(typeof XLSX==='undefined')throw new Error('No está disponible el lector del archivo de base.');if(!/^(xlsx|xls|xlsm|xlsb|ods)$/i.test(ext(file.name)))throw new Error('Selecciona un archivo compatible para la base.');const data=await file.arrayBuffer();const wb=XLSX.read(data,{type:'array',cellText:true,cellDates:false});const sheets=wb.SheetNames.map(name=>({name,rows:XLSX.utils.sheet_to_json(wb.Sheets[name],{header:1,raw:false,defval:''})}));return ingestWorkbookModel(file.name,sheets);}
  function ingestTxtText(fileName,text,{replace=true}={}){const parsed=Contract.parseTxt(text,fileName);if(replace){state.txtRecords=state.txtRecords.filter(r=>r.file!==fileName);state.errors=state.errors.filter(e=>!(e.scope==='TXT'&&e.file===fileName));}state.txtRecords.push(...parsed.records);state.txtSources.set(fileName,{records:parsed.records.length,totalLines:parsed.totalLines,errors:parsed.errors.length});state.errors.push(...parsed.errors.map(e=>({...e,scope:'TXT'})));rebuildIndex();updateStatus();refreshSearch();return parsed;}
  async function loadTxtFiles(files){const list=uniqueFiles(files);if(!list.length)throw new Error('No se encontraron archivos compatibles para comparar.');const failures=[];for(const file of list){try{ingestTxtText(file.webkitRelativePath||file.name,await file.text());}catch(error){failures.push({file:file.name,message:error.message||String(error),scope:'TXT'});}}state.errors.push(...failures);rebuildIndex();updateStatus();refreshSearch();return{loaded:list.length-failures.length,failed:failures.length};}
  function clearSources(){state.excelFile='';state.excelRecords=[];state.txtRecords=[];state.txtSources.clear();state.errors=[];rebuildIndex();updateStatus();refreshSearch();}

  const matchedFieldText=fields=>(fields||[]).map(f=>`${f.field}: ${f.value}`).join(' · ');
  function linkCard(link){const e=link.excel,t=link.txt;const sentence=link.exactPair?`El registro ${roleLabel(e.role)} ${e.srRaw||e.uaRaw} de ${e.file} (hoja “${e.sheet}”, fila ${e.rowNumber}) se encontró en ${t.file} (línea ${t.lineNumber}).`:`Coincidencia de identidad entre ${e.file} (hoja “${e.sheet}”, fila ${e.rowNumber}) y ${t.file} (línea ${t.lineNumber}) por ${link.matchKind}.`;return `<article class="cross-match-card ${link.exactPair?'exact':'related'}"><div class="cross-match-head"><span class="mini-badge ${link.exactPair?'ok':'warn'}">${link.exactPair?'CRUCE EXACTO':'CRUCE RELACIONADO'}</span><strong>${esc(roleLabel(e.role))}</strong></div><p class="cross-match-sentence">${esc(sentence)}</p><div class="cross-match-grid"><div><span>Base</span><strong>${esc(e.file)}</strong><small>Hoja: ${esc(e.sheet)} · fila ${e.rowNumber}</small><code>SR: ${esc(e.srRaw||'—')}</code><code>UA: ${esc(e.uaRaw||'—')}</code></div><div><span>Comparación</span><strong>${esc(t.file)}</strong><small>Línea: ${t.lineNumber}</small><code>CARCASA: ${esc(t.carcasaRaw||'—')}</code><code>EQUIPO: ${esc(t.equipoRaw||'—')}</code></div></div><div class="cross-match-proof"><span>${link.srMatch?'✓ SR = EQUIPO':'— SR ≠ EQUIPO'}</span><span>${link.uaMatch?'✓ UA = CARCASA':'— UA ≠ CARCASA'}</span></div></article>`;}
  function excelHitCard(e){return `<article class="cross-source-hit"><div><span class="mini-badge">BASE · ${esc(roleLabel(e.role))}</span><strong>${esc(e.file)}</strong></div><p>${esc(matchedFieldText(e.matchedFields))}</p><small>Hoja: <strong>${esc(e.sheet)}</strong> · fila <strong>${e.rowNumber}</strong> · SR: <code>${esc(e.srRaw||'—')}</code> · UA: <code>${esc(e.uaRaw||'—')}</code></small></article>`;}
  function txtHitCard(t){return `<article class="cross-source-hit"><div><span class="mini-badge">COMPARACIÓN</span><strong>${esc(t.file)}</strong></div><p>${esc(matchedFieldText(t.matchedFields))}</p><small>Línea <strong>${t.lineNumber}</strong> · CARCASA: <code>${esc(t.carcasaRaw||'—')}</code> · EQUIPO: <code>${esc(t.equipoRaw||'—')}</code></small></article>`;}
  function discoveryCard(h){const origin=h.source==='EXCEL'?`${h.file} · hoja ${h.sheet} · fila ${h.rowNumber} · columna ${h.field}`:`${h.file} · línea ${h.lineNumber} · columna ${h.field}`;return `<article class="cross-source-hit discovery-hit"><div><span class="mini-badge ${h.matchType==='EXACTO'?'ok':''}">ÍNDICE · ${esc(h.matchType)}</span><strong>${esc(h.value)}</strong></div><p>${esc(origin)}</p><small>Clasificación detectada: <strong>${esc(h.kind)}</strong>${h.source==='TXT'&&h.pairValid===false?' · línea genérica, no crea Match':''}</small></article>`;}

  function appendResults(query,resultsElement){
    if(!resultsElement)return;resultsElement.querySelectorAll('.cross-match-section').forEach(n=>n.remove());const q=String(query??'').trim();if(!q||!state.discoveryIndex.entries.length)return;
    const result=Contract.search(q,state.excelRecords,state.txtRecords,{discoveryIndex:state.discoveryIndex});
    const hasAny=result.excelHits.length||result.txtHits.length||result.links.length||result.discoveryHits.length;
    const linkedExcel=new Set(result.links.map(x=>`${x.excel.file}|${x.excel.sheet}|${x.excel.rowNumber}`));const linkedTxt=new Set(result.links.map(x=>`${x.txt.file}|${x.txt.lineNumber}`));
    const unlinkedExcel=result.excelHits.filter(e=>!linkedExcel.has(`${e.file}|${e.sheet}|${e.rowNumber}`));const unlinkedTxt=result.txtHits.filter(t=>!linkedTxt.has(`${t.file}|${t.lineNumber}`));
    const summary=hasAny?`<strong>${result.links.length} cruce${result.links.length===1?'':'s'}</strong> · ${result.discoveryHits.length} aparici${result.discoveryHits.length===1?'ón':'ones'} en índice`:'<strong>Sin coincidencia</strong> en las fuentes cargadas.';
    const body=`<section class="cross-match-section" aria-label="Cruce e índice de fuentes"><div class="cross-match-section-head"><span>CRUCE + ÍNDICE UNIVERSAL</span><div>${summary}</div></div>${result.links.map(linkCard).join('')}${unlinkedExcel.length?`<details class="cross-match-details" open><summary>Coincidencias estructuradas solo en la base (${unlinkedExcel.length})</summary>${unlinkedExcel.map(excelHitCard).join('')}</details>`:''}${unlinkedTxt.length?`<details class="cross-match-details" open><summary>Coincidencias estructuradas solo en comparación (${unlinkedTxt.length})</summary>${unlinkedTxt.map(txtHitCard).join('')}</details>`:''}${result.discoveryHits.length?`<details class="cross-match-details" open><summary>Todas las apariciones del índice (${result.discoveryHits.length})</summary>${result.discoveryHits.map(discoveryCard).join('')}</details>`:''}${!hasAny?'<div class="cross-match-empty">SIN COINCIDENCIA · Verifica el código o carga más fuentes.</div>':''}</section>`;
    resultsElement.insertAdjacentHTML('afterbegin',body);
  }

  function bind(){const excelBtn=$('#crossMatchExcelBtn'),txtBtn=$('#crossMatchTxtBtn'),folderBtn=$('#crossMatchFolderBtn'),clearBtn=$('#crossMatchClearBtn'),excelInput=$('#crossMatchExcelInput'),txtInput=$('#crossMatchTxtInput'),folderInput=$('#crossMatchFolderInput');if(!excelBtn||!txtBtn||!excelInput||!txtInput)return;excelBtn.addEventListener('click',()=>excelInput.click());txtBtn.addEventListener('click',()=>txtInput.click());folderBtn?.addEventListener('click',()=>folderInput?.click());clearBtn?.addEventListener('click',clearSources);excelInput.addEventListener('change',async()=>{try{await loadExcelFile(excelInput.files?.[0]);}catch(error){state.errors.push({file:excelInput.files?.[0]?.name||'Archivo de base',message:error.message||String(error),scope:'EXCEL'});updateStatus();}finally{excelInput.value='';}});txtInput.addEventListener('change',async()=>{try{await loadTxtFiles(txtInput.files);}catch(error){state.errors.push({file:'Archivo para comparar',message:error.message||String(error),scope:'TXT'});updateStatus();}finally{txtInput.value='';}});folderInput?.addEventListener('change',async()=>{try{await loadTxtFiles(folderInput.files);}catch(error){state.errors.push({file:'Carpeta para comparar',message:error.message||String(error),scope:'TXT'});updateStatus();}finally{folderInput.value='';}});updateStatus();}

  document.addEventListener('DOMContentLoaded',bind);
  window.MatchCrossSearch={version:VERSION,appendResults,loadExcelFile,loadTxtFiles,ingestWorkbookModel,ingestTxtText,clear:clearSources,getStats:()=>({excelFile:state.excelFile,excelRecords:state.excelRecords.length,txtFiles:state.txtSources.size,txtRecords:state.txtRecords.length,indexedValues:state.discoveryIndex.entries.length,errors:state.errors.length}),search:q=>Contract.search(q,state.excelRecords,state.txtRecords,{discoveryIndex:state.discoveryIndex})};
})();
